{
    "dependencies": {
        "concurrently": "^6.0.0"
    },
    "scripts": {
        "heroku-prebuild": "cd backend && yarn install && yarn run build",
        "start": "cd backend && yarn start"
    }
}
